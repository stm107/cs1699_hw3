My findings indicate that the local run of 3 small (10mb) files executed faster than the mapreduce version on pitt's hadoop cluster.

Notes on attached submission. My screenshot of the output for the local run is that of my terminal showing the execution/output files of the local run as well as a shot of the textfile output (also attached are the textfiles).

My screensots form the cluster run are as much as would fit on the screen and one that shows the time

ALL TIMES ARE REPRESENTED IN MILLISECONDS.
